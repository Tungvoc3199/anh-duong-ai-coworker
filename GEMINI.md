# Ánh Dương — Gemini Agent Entry

Before any Ánh Dương work, read `docs/AGENT_RUNTIME_CONTRACT.md` and run:

```bash
/usr/local/libexec/anh-duong/runtime-truth
```

Fresh runtime evidence overrides static documentation. Never mutate production or unrelated work before the contract's gates pass.
